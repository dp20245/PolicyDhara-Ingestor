name: Deploy to GitHub Pages (disabled)

# Dashboard removed — data is now committed directly to the repo as JSON.
# This workflow is kept to avoid breaking references but will never trigger.
on:
  workflow_dispatch:
    inputs:
      disabled:
        description: 'This workflow is disabled'
        required: false

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Dashboard deploy removed. Data lives in data/*.json"
